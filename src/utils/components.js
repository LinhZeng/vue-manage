import Vue from 'vue';
import betterDialog from '../components/Base/better-dialog';
import betterTable from '../components/Base/better-table';

Vue.component(betterDialog.name, betterDialog);
Vue.component(betterTable.name, betterTable);